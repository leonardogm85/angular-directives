import { FullHighlightDirective } from './full-highlight.directive';

describe('FullHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FullHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
